# دليل إدارة الحجوزات في Service Taxi Reservation System

يوضح هذا المستند دورة حياة الحجز بالكامل، منذ طلب الراكب وحتى تسجيل دخوله أو إلغائه، مع شرح الكيانات والوظائف المرتبطة.

---

## 1. الكيانات الأساسية
- **Reservation** (`models/Reservation.js`): يمثل الحجز نفسه ويحتوي على `bookingid`, `passengerid`, `tripid`, `seatlocation`, `bookingprice`, `status`, `booking_type`, إلخ.
- **Trip** (`models/Trip.js`): الرحلة التي يتم حجز مقعد فيها، تحتوي على السعة، حالة الرحلة، وقت الانطلاق، عدد الحجوزات.
- **Payment** (`models/Payment.js`): يسجل كل حركات الدفع المرتبطة بالحجز (محفظة، نقدًا، بوابات).
- **Wallet** (`models/Wallet.js`): لتسوية المدفوعات واسترجاع الأموال عند الحاجة.
- **Constants** (`utils/constants.js`): يعرف القيم المرجعية مثل `BOOKING_TYPE`, `PAYMENT_METHOD`, `RESERVATION_STATUS`, `CANCELLATION_POLICY`.
- **Helpers & Services**:
  - `utils/validation.js`: التحقق من بيانات الحجز.
  - `services/matchingService.js`: توزيع الحجوزات الفورية على السائقين بعد فتح الرحلة.
  - `utils/qrcode.js`: توليد QR لكل حجز.

---

## 2. أنواع الحجوزات
| النوع | الحقل | الاستخدام |
|-------|-------|-----------|
| فوري (`BOOKING_TYPE.INSTANT`) | `tripid` إلزامي | يرتبط برحلة مفتوحة حالًا، يتحقق من المقاعد المتاحة فورًا. |
| مستقبلي (`BOOKING_TYPE.FUTURE`) | `scheduled_trip_time`, `lineid` | حجز لرحلة مستقبلية وفق خط محدد؛ يتم مطابقة الوقت مع موعد الرحلة. |

---

## 3. إنشاء الحجز (`createReservation`)
1. **تحضير البيانات**:
   - يحدد النظام المستخدم الحالي عبر `req.user`.
   - يقرأ `tripid`, `seatlocation`, `dropoffpoint`, `booking_type`, `paymentmethod`.
2. **التحقق**:
   - دالة `validateReservationData` تتأكد من صلاحية البيانات الأساسية (مثلاً: التواريخ المستقبلية).
   - الحجوزات الفورية تتطلب `tripid`; المستقبلية تتطلب `scheduled_trip_time` و `lineid`.
   - عند تحديد مقعد (`seatlocation`) يتأكد النظام من عدم حجزه مسبقًا عبر `Reservation.getBySeatLocation`.
3. **تحديد السعر**:
   - يتم جلب بيانات الخط (`line`) لاستخراج `baseprice`.
   - يمكن إضافة `additionalprice` إذا تم اختيار نقطة إنزال (`dropoffpoint`).
4. **إنشاء سجل الدفع**:
   - ينشأ `Payment` بوضع `PENDING`.
   - إذا كان الدفع عبر المحفظة:
     - يتحقق من وجود محفظة ورصيد كافٍ.
     - يخصم الرصيد ويحدّث حالة الدفع إلى `COMPLETED`.
   - طرق الدفع الأخرى تبقى `PENDING` لحين المعالجة الخارجية.
5. **إدراج الحجز**:
   - ينشأ سجل `Reservation` مع حالة:
     - `CONFIRMED` إذا اكتمل الدفع (محفظة).
     - `PENDING` لغير ذلك.
   - يشمل `driver_status: 'pending'` إلى حين قبول السائق/النظام.
6. **تحديث الرحلة** (للحجوزات الفورية):
   - تقليل `availableseats` بواحد.
   - زيادة عدّاد `totalbookings`.
   - تشغيل محرك التوزيع الفوري `distributeInstantBookings` إذا كانت الرحلة قد فُتحت.
7. **توليد QR**:
   - باستخدام `generateQRCode` مع بيانات الحجز ليستخدم لاحقًا عند الصعود.
8. **الاستجابة**:
   - تعاد بيانات الحجز، حالة الدفع، و QR في استجابة 201.

**Fallback/استرجاع**:
- في حال حدوث استثناء:
  - تعاد الأموال إلى المحفظة إن تم خصمها.
  - تُحدّث حالة الدفع إلى `FAILED`.
  - يعاد المقعد المتاح وعدد الحجوزات إلى حالته السابقة.

---

## 4. استعلامات الحجز
- **الإدارة**: `getAllReservations` مع عوامل تصفية `status`, `tripid`.
- **الراكب**: `getPassengerReservations` يعرض الحجوزات حسب المستخدم الحالي ويمكن تقييدها بالحالة.
- **الاستعلام الفردي**: `getReservationById` يعيد تفاصيل الحجز مع التحقق من الوجود.

---

## 5. تحديث الحجز (`updateReservation`)
- يسمح بتعديل حقول محددة (مثل حالة السائق، وقت مجدول) عبر `Reservation.update`.
- يستخدم بحذر من قبل الإدارة أو الخدمات المؤتمتة.

---

## 6. إلغاء الحجز (`cancelReservation`)
1. التحقق من الوجود وعدم إلغائه مسبقًا.
2. تحديد موعد الانطلاق (`trip.deptime` أو `scheduled_trip_time`).
3. حساب الوقت المتبقي ومقدار الاسترجاع:
   - يستخدم `CANCELLATION_POLICY` + الدالة `calculateRefund`.
4. إعادة المبلغ (إن وجد) إلى محفظة المستخدم وإنشاء سجل `Payment` من نوع `refund`.
5. وضع حالة الحجز إلى `CANCELLED`.
6. تحرير مقعد الرحلة إذا كان الحجز مؤكدًا/Checked-in.

---

## 7. تسجيل الدخول للرحلة (Check-in)
- **المسار**: `checkInReservation`.
- **المستخدم**: السائق فقط؛ يتأكد من أن الرحلة تخص السائق نفسه.
- **المدخلات**: `bookingid` أو `qrData`.
- **العملية**:
  1. Parse QR إن وُجد لتحديد `bookingid`.
  2. التحقق من ملكية الرحلة للسائق (منع التلاعب).
  3. تحديث حالة الحجز إلى `CHECKED_IN`.

---

## 8. رمز QR للحجز
- `getReservationQRCode` يسمح للراكب بتحميل QR في أي وقت.
- يتحقق من ملكية الحجز قبل إنشاء الرمز لضمان الخصوصية.
- يستخدم عند الصعود لتمريره للسائق عبر تطبيق مستقل أو نفس تطبيق الراكب.

---

## 9. إدارة السعة والمقاعد
- `Trip.updateAvailableSeats` و `Trip.incrementBookings` يستخدمان للحفاظ على الاتساق.
- عند الفشل أو الإلغاء يعاد المقعد تلقائيًا.
- `utils/seatLayout.js` يقدم نماذج جاهزة لمخططات المقاعد (مهم لتطبيقات الواجهة الأمامية).

---

## 10. حالات الحجز (`RESERVATION_STATUS`)
| الحالة | الوصف | من يغيرها |
|---------|--------|-----------|
| `PENDING` | بانتظار الدفع أو موافقة السائق | النظام عند إنشاء حجز بدفع غير مكتمل |
| `CONFIRMED` | تم الدفع والتأكيد | النظام بعد خصم المحفظة أو بوابة الدفع |
| `CHECKED_IN` | الركاب صعدوا للحافلة | السائق عبر `checkInReservation` |
| `CANCELLED` | الحجز أُلغي | الراكب/الإدارة/النظام حسب السياسات |
| `NO_SHOW` | الراكب لم يحضر | وظائف `noShowCheckJob` أو المشغل اليدوي |

---

## 11. الربط مع المهام المجدولة
- **`noShowCheckJob`**: يغيّر الحالة إلى `NO_SHOW` للحجوزات التي لم تُسجل دخولًا بعد حدود زمنية محددة.
- **`tripOpeningJob` / `departureCheckJob`**: تتأكد من جاهزية الرحلة مما يسمح للحجوزات الفورية بالتفعيل.
- **تحذير**: إذا تعطلت إحدى الوظائف، قد تبقى الحجوزات في حالة خاطئة، لذلك راقب السجلات.

---

## 12. سيناريوهات شائعة
1. **حجز فوري ناجح**:
   - الراكب يختار رحلة مفتوحة، يدفع من المحفظة، يحصل على QR، تقل المقاعد.
2. **حجز مستقبلي**:
   - يحدد خطًا ووقتًا، ينتظر إنشاء الرحلة الفعلية، وعند توافق الوقت يتم تأكيده عبر معالجة إضافية (غير متضمنة هنا).
3. **إلغاء قبل 4 ساعات**:
   - يتم احتساب نسبة استرجاع حسب السياسة، تعاد الأموال إلى المحفظة، ويتاح المقعد من جديد.
4. **راكب لم يحضر**:
   - بعد تشغيل `noShowCheckJob` يتم تحديث الحالة إلى `NO_SHOW`، ويمكن تطبيق غرامات حسب المتطلبات.

---

## 13. نصائح للتمديد
- أضف إشعارات (SMS/Push) عند تأكيد الحجز أو تغييره.
- ربط بوابات دفع حقيقية ضمن `paymentController` وتحويل الحالات من `PENDING` إلى `COMPLETED`.
- تنفيذ قوائم انتظار ديناميكية للرحلات الممتلئة وتحويل الحجوزات تلقائيًا عند توفر مقاعد.
- إضافة تحليلات لقياس نسب الإلغاء، نسب الحضور، ومتوسط وقت الحجز.

> يُحدث هذا الملف كلما تم تعديل منطق الحجوزات لضمان فهم موحد بين فريق التطوير والتشغيل.


