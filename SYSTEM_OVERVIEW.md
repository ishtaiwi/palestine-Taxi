# دليل تشغيل مشروع Service Taxi Reservation System

يوضح هذا الملف كيفية عمل النظام بالتفصيل من لحظة استلام الطلب وحتى تنفيذ جميع العمليات الخلفية.

---

## 1. نظرة عامة سريعة
- **الغرض**: حجز وإدارة سيارات الخدمة (السيرفس) في فلسطين، مع دعم السائقين، الركاب، والإدارة.
- **التقنيات الرئيسية**: `Node.js`, `Express`, `Supabase/PostgreSQL`, `JWT`, `node-cron`, وواجهة Flutter موجودة داخل مجلد `frontend/`.
- **الواجهة البرمجية**: جميع المسارات تبدأ بـ `/api/`، وتوثيقها متاح عبر `GET /api/docs`.

---

## 2. الطبقات الأساسية في المشروع
| الطبقة | المكان | الدور |
|--------|--------|-------|
| HTTP/API | `server.js`, `routes/` | استقبال الطلبات، تطبيق الـ Middleware، وتمريرها للـ Controllers. |
| Controllers | `controllers/*.js` | منطق الأعمال لكل مجال (رحلات، حجوزات، مدفوعات...). |
| Models | `models/*.js` | التعامل المباشر مع Supabase (CRUD، استعلامات مركبة). |
| خدمات مساعدة | `services/`, `utils/` | مهام خاصة مثل حساب المقاعد، إرسال البريد، تسجيل السجلات. |
| Middleware | `middleware/` | مصادقة JWT، تفويض الأدوار، الترجمة، التعامل مع الأخطاء، تحديد المعدل. |
| المهام المجدولة | `jobs/*.js` | أتمتة فتح الرحلات، التحقق من الانطلاق، إنشاء رحلات اليوم التالي، رصد المتغيبين. |

---

## 3. تسلسل الطلب القياسي
1. **العميل** (تطبيق Flutter أو أي مستهلك آخر) يرسل طلبًا إلى `/api/...`.
2. **طبقة CORS والـ Middleware** تضبط اللغة، تفرض حدود المعدل، وتتحقق من صحة التوكن إن وجِد.
3. **الراوتر** يختار الـ Controller بحسب المسار (مثلاً `/api/trips` → `tripController`).
4. **الـ Controller** يجمع البيانات من الجسم/المسار، يتحقق من الصلاحيات، ثم يستدعي الموديل أو الخدمة.
5. **الموديل** ينفذ استعلام Supabase ويرجع النتيجة.
6. **الـ Controller** يصيغ الرد في JSON موحد (مع رسائل مترجمة عبر `req.t`) ويعيده للعميل.

---

## 4. المصادقة وإدارة الهوية
- التسجيل وتسجيل الدخول عبر `authController`:
  - عند التسجيل يتم إنشاء مستخدم في Supabase وتوليد JWT.
  - تسجيل الدخول يتحقق من البريد وكلمة المرور باستخدام `bcryptjs`.
- **JWT**:
  - يوقَّع باستخدام `config/app.js` (`jwt.secret`, `jwt.expiresIn`).
  - يحقن في رأس `Authorization: Bearer <token>`.
  - Middleware `auth.js` يفك التوكن ويضع بيانات المستخدم في `req.user`.
- **تفويض الأدوار**:
  - Middleware `authorization.js` يضمن أن الدور (مثل `admin`, `driver`, `passenger`) مخوّل لتنفيذ المسار.

---

## 5. إدارة الخطوط والرحلات
- **الخطوط (`lineController`)**:
  - إنشاء/تعديل/حذف خطوط، تحديد نقاط الانطلاق والوصول، السعر الأساسي، والزمن.
  - تستخدم كنقطة مرجعية للرحلات المجدولة.
- **الرحلات (`tripController`)**:
  - إنشاء رحلات يومية بناءً على قوالب الجداول (يدويًا أو عبر job تلقائية).
  - كل رحلة تحتوي على `tripid`, `lineid`, `driverid`, `vehicleid`, حالة (مفتوح، في الطريق، منتهٍ).
  - تحديث حالة الرحلة يحدث تلقائيًا عبر jobs أو يدويًا من قبل الإدارة.

---

## 6. الحجز وإدارة المقاعد
- **الحجوزات (`reservationController`)**:
  - إنشاء حجز يحدد `tripid`, معلومات الراكب, وعدد المقاعد.
  - التحقق من السعة المتاحة يتم عبر `utils/seatCalculation.js`.
  - عند امتلاء الرحلة يمكن إضافة الراكب لقائمة انتظار (حسب الإعدادات).
- **إدارة المقاعد**:
  - يتم تحديث `availableSeats` و `occupiedSeats` في الرحلة عند كل حجز/إلغاء.
  - توفر النظام نماذج مقاعد عبر `utils/seatLayout.js` لدعم مركبات مختلفة.

---

## 7. المدفوعات والمحافظ
- **المحافظ (`walletController`)**:
  - لكل مستخدم محفظة رئيسية (`type: main`) تنشأ تلقائيًا عند الحاجة.
  - يمكن الإيداع مباشرة أو تحويل رصيد بين المحافظ (وفق صلاحيات).
  - كل عملية تُسجّل في جدول `Payment`.
- **المدفوعات (`paymentController`)**:
  - تجريد لبوابات الدفع مثل PalPay و JawwalPay (المفاتيح في `config/app.js`).
  - تتبع حالة الدفع (`PENDING`, `COMPLETED`, `FAILED`) لتحديث الحجز أو المحفظة.
- **التدفق المعتاد**:
  1. الراكب يحجز → يُطلب الدفع أو يستخدم رصيد المحفظة.
  2. عند النجاح، يتم خصم المبلغ وتأكيد الحجز.
  3. في حال الفشل، يبقى الحجز في حالة انتظار أو يُلغى حسب السياسة.

---

## 8. تقييم الرحلات
- **`ratingController`** يسمح للراكب بتقييم الرحلة بعد انتهائها.
- يتم ربط التقييم بـ `tripid` و `driverid`، وتجميع النتائج لتقديم إحصائيات للسائقين والإدارة.

---

## 9. المهام الخلفية (node-cron)
| الملف | التوقيت/المهمة | ما الذي يحدث |
|-------|----------------|--------------|
| `tripOpeningJob.js` | دوريًا قبل موعد الرحلة | يغيّر حالة الرحلات من "مجدولة" إلى "مفتوحة" ويبلغ السائقين. |
| `departureCheckJob.js` | عند أوقات الانطلاق | يتحقق إن كانت الرحلة غادرت فعليًا، وإلا يسجل تأخيرًا أو ينبه الإدارة. |
| `noShowCheckJob.js` | بعد الانطلاق بفترة | يحدد الركاب المتغيبين ويعيد المقاعد أو يحدث الحالة المالية. |
| `dailyTripCreationJob.js` | مرة يوميًا | ينشئ رحلات الغد بناءً على قوالب `scheduleController`. |

> تبدأ جميع الـ jobs داخل `server.js` فقط إذا نجح `testConnection` مع Supabase لضمان عدم تشغيل مهام بلا قاعدة بيانات.

---

## 10. البيئة والتشغيل
1. **المتطلبات**: Node.js 18+, npm، حساب Supabase، ملف `.env`.
2. **إعداد البيئة**:
   - انسخ `config/app.js` كمرجع وحدد جميع المفاتيح في `.env`.
   - شغّل سكربتات `migrations/*.sql` على قاعدة Supabase لإنشاء الجداول.
3. **تشغيل الخادم**:
   ```bash
   npm install
   npm run dev   # يستخدم nodemon
   # أو
   npm start     # لتشغيل عادي
   ```
4. **التحقق**:
   - `GET /health` يجب أن يعيد حالة OK.
   - سجلات الطرفية (عبر `utils/logger.js`) تؤكد نجاح الاتصال وتشغيل الـ jobs.

---

## 11. تكامل الواجهة الأمامية
- تطبيق Flutter (مجلد `frontend/`) يتصل بالـ API عبر `http`/`dio`.
- خطوات عامة:
  1. استدعاء `/api/auth/login` للحصول على JWT.
  2. تخزين التوكن وتمريره في كل طلب محمي.
  3. استخدام `/api/lines` لعرض الخطوط، ثم `/api/trips` و `/api/reservations` للحجوزات.
  4. مراقبة الحالة لحظيًا عبر استدعاءات متكررة أو إشعارات مستقبلًا (غير مطبق حاليًا).
- ملفات `BACKEND_FRONTEND_INTEGRATION.md` توفر تفاصيل أوضح لكل شاشة.

---

## 12. استكشاف الأخطاء وإصلاحها
- **المهام لا تعمل**: تحقق من السجلات عند تشغيل الخادم؛ إذا فشل الاتصال بـ Supabase فلن تبدأ الـ jobs.
- **مشاكل CORS**: عدّل `CORS_ORIGIN` في `.env` أو `config/app.js` لتشمل نطاق الواجهة الأمامية.
- **أخطاء قاعدة البيانات**: راجع `config/dbcon.js`؛ الدالة `testConnection` تعيد سبب الفشل في `/api/db/test`.
- **انقطاع البريد أو الدفع**: تحقق من بيانات `EMAIL_*` و `PALPAY_API_KEY`/`JAWWAL_PAY_API_KEY`.
- **مزامنة الوقت**: جزء كبير من المنطق يعتمد على الوقت المحلي، لذا تأكد أن الخادم يملك توقيتًا صحيحًا.

---

## 13. سيناريو يوم نموذجي
1. منتصف الليل: `dailyTripCreationJob` ينشئ رحلات اليوم حسب القوالب.
2. صباحًا: السائقون يتلقون إشعارًا بالرحلات المفتوحة (`tripOpeningJob`).
3. الركاب يحجزون مقاعدهم ويدفعون عبر المحفظة أو البوابة.
4. قبل الانطلاق: `departureCheckJob` يتأكد أن كل رحلة بدأت في الوقت المحدد.
5. بعد الانطلاق: `noShowCheckJob` يحرر مقاعد الغائبين أو يخصم رسومهم.
6. نهاية الرحلة: يتمكن الركاب من تقييم التجربة؛ النتائج تُعرض للإدارة لتقييم السائق.

---

## 14. مصادر إضافية
- `README.md`: نظرة مختصرة ومتطلبات التشغيل.
- `BACKEND_FRONTEND_INTEGRATION.md`: تفصيل واجهات Flutter وكيفية استهلاك الـ API.
- `IMPLEMENTATION_ROADMAP.md`: خطة التطوير والمراحل التاريخية.

> هذا الدليل مصمم ليكون مرجعًا شاملًا يوضح كيفية عمل المشروع من الداخل. قم بتحديثه كلما تغيرت البنية أو أضيفت خدمات جديدة.


